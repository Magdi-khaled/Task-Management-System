import { createStore } from 'vuex';

// Define the store
const authStore = createStore({
    state() {
        return {};
    },
    mutations: {},
    actions: {},
    getters: {},
});

// Export the store
export default authStore;
